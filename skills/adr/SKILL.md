---
name: adr
description: 当架构决策难以逆转、脱离上下文会令人困惑，且是真实权衡的结果时，记录持久的架构决策。
---

# ADR

当设计过程中重大架构决策逐渐明确时，记录重要的架构决策。

## 资格条件

仅当以下三个条件全部满足时，才创建 ADR：

1. **难以逆转** — 以后更改该决策具有有意义的成本
2. **脱离上下文会令人困惑** — 未来的读者很可能会合理地想知道为什么选择了这种方法
3. **是真实权衡的结果** — 存在真正的备选方案，并且基于特定原因选择了其中一个

如果任一条件不满足，则不要创建 ADR。

典型的合格决策包括：

- 架构形态
- 上下文之间的集成模式
- 具有有意义锁定效应的技术选择
- 所有权和边界决策
- 对显而易见路径的故意偏离
- 代码中不可见的约束
- 对看似合理的备选方案的非明显拒绝

不要为本地实现细节、显而易见的决策或容易逆转的决策创建 ADR。

## 位置

将 ADR 存储在 `docs/adr/` 中，使用顺序编号：

`0001-slug.md`，`0002-slug.md`，...

对于包含多个上下文的代码库，将特定于上下文的 ADR 放在该上下文中，系统级 ADR 放在 ADR 根目录。

按需创建 ADR 目录。

## 编号

扫描目标 ADR 目录中存在的最大编号，并将其加一。

## 格式

默认保持 ADR 最小化：

```md
# {决策的简短标题}

{1-3 句话描述上下文、决策以及选择它的原因。}